# The versions the reported numbers were produced on (Python 3.12.10).
# Pinned so the figures and the report's numbers reproduce, not because any newer
# version is known to break anything.
pandas==3.0.5
numpy==2.5.3
statsmodels==0.15.0
scikit-learn==1.9.1
matplotlib==3.11.2
pyarrow==25.0.1

# The suite runs in CI, which installs only this file.
pytest==9.1.1
